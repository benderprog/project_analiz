from django.apps import AppConfig


class PortaldbConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.portaldb"
    label = "portaldb"
    verbose_name = "Portal DB"
