# Offline compose assets

Templates for the offline release bundle are stored here.

Use `scripts/offline/offline.sh bundle ...` to generate a runnable bundle in `dist/` and follow the full runbook in `doc/offline/closed_contour_1_5_test.md`.
